service: aws-python-http-api-project-personal
frameworkVersion: '3'

plugins:
  - serverless-offline

provider:
  name: aws
  runtime: python3.9

functions:
  info:
    handler: handler.info
    events:
      - http:
          path: /info
          method: get
